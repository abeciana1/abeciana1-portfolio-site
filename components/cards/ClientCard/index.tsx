import React from 'react'

interface ClientI {
    name: string;
    bio: string;
    link: string;
    logo: string;
}

const ClientCard = ({
    name,
    bio,
    link,
    logo
}: ClientI) => {
    
    return (
        <React.Fragment>
            <div></div>
        </React.Fragment>
    )
}